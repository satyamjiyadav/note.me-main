import React from 'react'
import styles from "./modernButton.module.scss"

function ModernButton() {
  return (
    <button className={styles.container}>Pick Up</button>
  )
}

export default ModernButton